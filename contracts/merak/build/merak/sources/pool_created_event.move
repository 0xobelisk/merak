  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: Apache-2.0
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::pool_created_event {

  use sui::event;

  use std::ascii::String;

  use merak::account_status::AccountStatus;

  use merak::asset_status::AssetStatus;

  use merak::account::Account;

  use merak::asset_metadata::AssetMetadata;

  use merak::asset_details::AssetDetails;

  use merak::pool::Pool;

  use merak::path_element::PathElement;

  public struct PoolCreatedEvent has copy, drop {
    creator: address,
    asset1_id: u128,
    asset2_id: u128,
    pool_address: address,
    lp_asset_id: u128,
    lp_asset_symbol: String,
  }

  public fun new(
    creator: address,
    asset1_id: u128,
    asset2_id: u128,
    pool_address: address,
    lp_asset_id: u128,
    lp_asset_symbol: String,
  ): PoolCreatedEvent {
    PoolCreatedEvent {
                                   creator,asset1_id,asset2_id,pool_address,lp_asset_id,lp_asset_symbol
                               }
  }
}
