  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: MIT
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::dex_liquidity_removed_event {

  use sui::event;

  use std::ascii::String;

  public struct LiquidityRemovedEvent has copy, drop {
    who: address,
    pool_id: u32,
    asset1_amount: u256,
    asset2_amount: u256,
    lp_asset_id: u32,
    lp_asset_burned: u256,
  }

  public fun new(
    who: address,
    pool_id: u32,
    asset1_amount: u256,
    asset2_amount: u256,
    lp_asset_id: u32,
    lp_asset_burned: u256,
  ): LiquidityRemovedEvent {
    LiquidityRemovedEvent {
                                   who,pool_id,asset1_amount,asset2_amount,lp_asset_id,lp_asset_burned
                               }
  }

  public fun emit(
    who: address,
    pool_id: u32,
    asset1_amount: u256,
    asset2_amount: u256,
    lp_asset_id: u32,
    lp_asset_burned: u256,
  ) {
    event::emit(LiquidityRemovedEvent {
                                   who,pool_id,asset1_amount,asset2_amount,lp_asset_id,lp_asset_burned
                               });
  }
}
