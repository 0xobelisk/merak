  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: Apache-2.0
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::merak_liquidity_added_event {

  use sui::event;

  use std::ascii::String;

  use merak::merak_account_status::AccountStatus;

  use merak::merak_asset_status::AssetStatus;

  use merak::merak_asset_type::AssetType;

  use merak::merak_account::Account;

  use merak::merak_asset_metadata::AssetMetadata;

  use merak::merak_pool::Pool;

  use merak::merak_path_element::PathElement;

  use merak::merak_bridge_config::BridgeConfig;

  public struct LiquidityAddedEvent has copy, drop {
    who: address,
    asset1_id: u256,
    asset2_id: u256,
    asset1_amount: u256,
    asset2_amount: u256,
    lp_asset_id: u256,
    lp_asset_minted: u256,
  }

  public fun new(
    who: address,
    asset1_id: u256,
    asset2_id: u256,
    asset1_amount: u256,
    asset2_amount: u256,
    lp_asset_id: u256,
    lp_asset_minted: u256,
  ): LiquidityAddedEvent {
    LiquidityAddedEvent {
                                   who,asset1_id,asset2_id,asset1_amount,asset2_amount,lp_asset_id,lp_asset_minted
                               }
  }
}
