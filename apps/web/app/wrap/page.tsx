'use client';

import React from 'react';
import TokenWrapper from '@/app/components/wrap/wrap';

export default function Page() {
  return (
    <main className="flex flex-col items-center justify-center flex-1 min-h-[calc(100vh-64px)]">
      <TokenWrapper />
    </main>
  );
}
