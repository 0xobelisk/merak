  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: MIT
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::dex_swap_executed_event {

  use sui::event;

  use std::ascii::String;

  public struct SwapExecutedEvent has copy, drop {
    who: address,
    send_to: address,
    amount_in: u256,
    amount_out: u256,
    path: vector<u32>,
  }

  public fun new(
    who: address,
    send_to: address,
    amount_in: u256,
    amount_out: u256,
    path: vector<u32>,
  ): SwapExecutedEvent {
    SwapExecutedEvent {
                                   who,send_to,amount_in,amount_out,path
                               }
  }

  public fun emit(who: address, send_to: address, amount_in: u256, amount_out: u256, path: vector<u32>) {
    event::emit(SwapExecutedEvent {
                                   who,send_to,amount_in,amount_out,path
                               });
  }
}
