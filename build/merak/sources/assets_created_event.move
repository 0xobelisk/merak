  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: MIT
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::assets_created_event {

  use sui::event;

  use std::ascii::String;

  public struct CreatedEvent has copy, drop {
    asset_id: u32,
    name: String,
    symbol: String,
    owner: address,
    is_mintable: bool,
    is_burnable: bool,
    is_freezable: bool,
  }

  public fun new(
    asset_id: u32,
    name: String,
    symbol: String,
    owner: address,
    is_mintable: bool,
    is_burnable: bool,
    is_freezable: bool,
  ): CreatedEvent {
    CreatedEvent {
                                   asset_id,name,symbol,owner,is_mintable,is_burnable,is_freezable
                               }
  }

  public fun emit(
    asset_id: u32,
    name: String,
    symbol: String,
    owner: address,
    is_mintable: bool,
    is_burnable: bool,
    is_freezable: bool,
  ) {
    event::emit(CreatedEvent {
                                   asset_id,name,symbol,owner,is_mintable,is_burnable,is_freezable
                               });
  }
}
