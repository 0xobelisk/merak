  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: MIT
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::wrapper_registered_event {

  use sui::event;

  use std::ascii::String;

  public struct RegisteredEvent has copy, drop {
    who: address,
    asset_id: u32,
  }

  public fun new(who: address, asset_id: u32): RegisteredEvent {
    RegisteredEvent {
                                   who,asset_id
                               }
  }

  public fun emit(who: address, asset_id: u32) {
    event::emit(RegisteredEvent {
                                   who,asset_id
                               });
  }
}
