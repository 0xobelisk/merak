  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: MIT
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module merak::dex_liquidity_added_event {

  use sui::event;

  use std::ascii::String;

  public struct LiquidityAddedEvent has copy, drop {
    who: address,
    pool_id: u32,
    asset1_amount: u64,
    asset2_amount: u64,
    lp_asset_id: u32,
    lp_asset_minted: u64,
  }

  public fun new(
    who: address,
    pool_id: u32,
    asset1_amount: u64,
    asset2_amount: u64,
    lp_asset_id: u32,
    lp_asset_minted: u64,
  ): LiquidityAddedEvent {
    LiquidityAddedEvent {
                                   who,pool_id,asset1_amount,asset2_amount,lp_asset_id,lp_asset_minted
                               }
  }

  public fun emit(
    who: address,
    pool_id: u32,
    asset1_amount: u64,
    asset2_amount: u64,
    lp_asset_id: u32,
    lp_asset_minted: u64,
  ) {
    event::emit(LiquidityAddedEvent {
                                   who,pool_id,asset1_amount,asset2_amount,lp_asset_id,lp_asset_minted
                               });
  }
}
